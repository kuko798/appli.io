# Local OpenAI-compatible chat for Appli.io (Deep Scan, interview sim, JSON tools).
# Install: pip install -r requirements.txt
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
